---
layout: layouts/base.njk
section: page
---
<h1>{{ title }}</h1>

{{ layoutContent | safe }}

{% for service in services %}
<a href="/services/{{ service.name | slug }}">{{service.name}}</a> 
{% endfor %}